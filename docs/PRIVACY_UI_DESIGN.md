# プライバシー機能 UI/UX 設計書

**作成日**: 2025-12-18
**対象バージョン**: v1.1.0
**目的**: ユーザーに学習機能を説明し、同意を取得するUI/UX設計

---

## 📋 目次

1. [概要](#1-概要)
2. [画面フロー](#2-画面フロー)
3. [UI設計](#3-ui設計)
4. [実装ファイル一覧](#4-実装ファイル一覧)
5. [技術仕様](#5-技術仕様)

---

## 1. 概要

### 1.1 目的

- ユーザーに学習機能を明確に説明
- 個人情報保護法・GDPRに準拠した同意取得
- いつでも同意を撤回・データ削除できる機能

### 1.2 設計原則

| 原則 | 詳細 |
|------|------|
| **透明性** | データ収集内容を明示 |
| **選択権** | ユーザーが選択できる |
| **削除可能** | いつでもデータ削除可能 |
| **わかりやすさ** | 専門用語を避ける |

---

## 2. 画面フロー

### 2.1 初回起動時

```
アプリ起動
    ↓
【v1.0.84 既存】権限リクエスト
    - Overlay権限
    - Accessibility権限
    - Screen Capture権限
    ↓
【v1.1.0 NEW】学習機能の説明ダイアログ
    ↓
ユーザー選択:
    [同意する] → MainActivity（学習機能ON）
    [同意しない] → MainActivity（学習機能OFF）
    [ポリシーを見る] → プライバシーポリシー画面 → 戻る
```

### 2.2 設定画面からの操作

```
設定画面
    ↓
学習機能セクション
    ├─ 学習機能のON/OFF切り替え
    ├─ 学習パターン数の表示
    ├─ プライバシーポリシーを見る
    └─ 学習データを削除
        ↓
    削除確認ダイアログ
        ↓
    削除完了（Toast表示）
```

---

## 3. UI設計

### 3.1 初回同意ダイアログ

#### デザインモックアップ

```
┌──────────────────────────────────────┐
│  🔒 学習機能について                     │
├──────────────────────────────────────┤
│                                      │
│  このアプリは、OCR認識の精度向上のため、  │
│  あなたの修正内容をローカルに保存します。│
│                                      │
│  ✅ データはデバイス内のみに保存         │
│  ✅ 外部サーバーには送信しません         │
│  ✅ 第三者と共有しません                 │
│  ✅ いつでも削除できます                 │
│                                      │
│  収集する情報:                         │
│  • OCRテキスト（修正前）                │
│  • 修正後のテキスト                     │
│  • 使用回数と日時                      │
│                                      │
│  収集しない情報:                       │
│  • 書籍タイトル、著者名                 │
│  • ページ番号、位置情報                 │
│  • その他の個人情報                     │
│                                      │
│  詳細: プライバシーポリシーをご覧ください  │
│                                      │
├──────────────────────────────────────┤
│  [ポリシーを見る]  [同意しない] [同意する]│
└──────────────────────────────────────┘
```

#### UI要素

| 要素 | 種類 | 説明 |
|------|------|------|
| **タイトル** | TextView | "🔒 学習機能について" |
| **本文** | ScrollView + TextView | 説明文（上記参照） |
| **ポリシーボタン** | TextButton | プライバシーポリシー画面へ遷移 |
| **拒否ボタン** | TextButton | 学習機能OFF、ダイアログ閉じる |
| **同意ボタン** | FilledButton（強調） | 学習機能ON、ダイアログ閉じる |

#### UXポイント

- ✅ **強制しない**: 拒否してもアプリは使用可能
- ✅ **わかりやすい**: 箇条書きで簡潔に説明
- ✅ **選択肢明確**: 3つのボタンで選択可能
- ✅ **再表示可能**: 設定画面からいつでも表示

---

### 3.2 設定画面の学習機能セクション

#### デザインモックアップ

```
┌──────────────────────────────────────┐
│  設定                                 │
├──────────────────────────────────────┤
│                                      │
│  【音声読み上げ】                      │
│    音声速度: 1.0x                     │
│    音量: 80%                         │
│                                      │
│  【学習機能】✨ NEW                    │
│    ┌────────────────────────────┐  │
│    │ 学習機能を使用する    [ON/OFF] │  │
│    │                                │  │
│    │ 学習パターン数: 125件           │  │
│    │ 最終更新: 2025-12-18           │  │
│    │                                │  │
│    │ [プライバシーポリシーを見る]     │  │
│    │ [学習データを削除]              │  │
│    └────────────────────────────┘  │
│                                      │
│  【アクセシビリティ】                  │
│    自動ページめくり: ON               │
│                                      │
└──────────────────────────────────────┘
```

#### UI要素

| 要素 | 種類 | 説明 |
|------|------|------|
| **セクションタイトル** | PreferenceCategory | "学習機能" + ✨ NEW バッジ |
| **ON/OFF切り替え** | SwitchPreferenceCompat | 学習機能の有効/無効 |
| **パターン数表示** | Preference (read-only) | 現在の学習パターン数 |
| **最終更新日表示** | Preference (read-only) | 最後に学習した日時 |
| **ポリシーボタン** | Preference (clickable) | プライバシーポリシー画面へ |
| **削除ボタン** | Preference (clickable) | 削除確認ダイアログ表示 |

---

### 3.3 学習データ削除確認ダイアログ

#### デザインモックアップ

```
┌──────────────────────────────────────┐
│  ⚠️  学習データの削除                   │
├──────────────────────────────────────┤
│                                      │
│  すべての学習データを削除します。        │
│  この操作は取り消せません。             │
│                                      │
│  削除されるデータ:                     │
│  • 修正履歴: 125件                    │
│  • 統計情報                           │
│                                      │
│  削除されないデータ:                   │
│  • アプリの設定                        │
│  • OCR補正機能（Gemini API）          │
│                                      │
│  よろしいですか？                      │
│                                      │
├──────────────────────────────────────┤
│           [キャンセル]  [削除]         │
└──────────────────────────────────────┘
```

#### UI要素

| 要素 | 種類 | 説明 |
|------|------|------|
| **タイトル** | TextView | "⚠️ 学習データの削除" |
| **本文** | TextView | 削除内容の説明 |
| **キャンセルボタン** | TextButton | ダイアログを閉じる |
| **削除ボタン** | FilledTonalButton（警告色） | データ削除を実行 |

#### UXポイント

- ⚠️ **警告色**: 削除ボタンは赤系の警告色
- ℹ️ **詳細表示**: 削除される/されないデータを明示
- ✅ **確認必須**: ワンタップで削除しない

---

### 3.4 プライバシーポリシー表示画面

#### デザインモックアップ

```
┌──────────────────────────────────────┐
│  ← プライバシーポリシー                 │
├──────────────────────────────────────┤
│                                      │
│  【プライバシーポリシー全文】           │
│  （ScrollView）                       │
│                                      │
│  1. はじめに                          │
│     Kindle TTS Readerは...          │
│                                      │
│  2. 運営者情報                        │
│     アプリ名: Kindle TTS Reader      │
│     ...                              │
│                                      │
│  （以下、PRIVACY_POLICY_ja.mdの内容） │
│                                      │
│                                      │
│                                      │
├──────────────────────────────────────┤
│           [閉じる] [同意する]          │
└──────────────────────────────────────┘
```

#### UI要素

| 要素 | 種類 | 説明 |
|------|------|------|
| **タイトルバー** | TopAppBar | "プライバシーポリシー" + 戻るボタン |
| **本文** | ScrollView + Markdown | PRIVACY_POLICY_ja.mdの内容 |
| **閉じるボタン** | TextButton | 画面を閉じる |
| **同意ボタン** | FilledButton | 学習機能ONして閉じる（初回のみ表示） |

#### 実装方法

| オプション | メリット | デメリット |
|-----------|---------|----------|
| **WebView** | Markdownをそのまま表示 | サイズ大、読み込み遅い |
| **TextView（HTML）** | 軽量、高速 | Markdown変換が必要 |
| **Markwon library** | Markdown完全サポート | 依存関係追加 |

**推奨**: **Markwon library**（Markdown表示専用ライブラリ）

```gradle
dependencies {
    implementation 'io.noties.markwon:core:4.6.2'
}
```

---

## 4. 実装ファイル一覧

### 4.1 新規作成ファイル

| ファイルパス | 目的 |
|------------|------|
| `app/src/main/java/com/kindletts/reader/ui/PrivacyConsentDialog.kt` | 初回同意ダイアログ |
| `app/src/main/java/com/kindletts/reader/ui/PrivacyPolicyActivity.kt` | プライバシーポリシー表示画面 |
| `app/src/main/res/layout/dialog_privacy_consent.xml` | 同意ダイアログレイアウト |
| `app/src/main/res/layout/activity_privacy_policy.xml` | ポリシー画面レイアウト |
| `app/src/main/res/values/strings_privacy.xml` | プライバシー関連の文字列リソース |
| `app/src/main/res/xml/preferences_learning.xml` | 設定画面の学習機能セクション |

### 4.2 既存ファイルの修正

| ファイルパス | 修正内容 |
|------------|---------|
| `app/src/main/java/com/kindletts/reader/MainActivity.kt` | 初回起動時にPrivacyConsentDialog表示 |
| `app/src/main/java/com/kindletts/reader/SettingsActivity.kt` | 学習機能セクション追加 |
| `app/src/main/AndroidManifest.xml` | PrivacyPolicyActivity追加 |
| `app/build.gradle` | Markwon依存関係追加 |

---

## 5. 技術仕様

### 5.1 同意状態の管理

#### SharedPreferencesキー

```kotlin
object PrivacyPreferences {
    const val PREF_NAME = "privacy_prefs"

    // 同意状態
    const val KEY_LEARNING_CONSENT = "learning_consent_given"  // Boolean
    const val KEY_CONSENT_DATE = "consent_date"                // Long (timestamp)
    const val KEY_CONSENT_VERSION = "consent_policy_version"   // String (e.g., "v1.0")

    // ダイアログ表示状態
    const val KEY_CONSENT_DIALOG_SHOWN = "consent_dialog_shown"  // Boolean
}
```

#### 同意状態の判定ロジック

```kotlin
fun hasUserConsented(): Boolean {
    val prefs = getSharedPreferences(PrivacyPreferences.PREF_NAME, MODE_PRIVATE)
    return prefs.getBoolean(PrivacyPreferences.KEY_LEARNING_CONSENT, false)
}

fun shouldShowConsentDialog(): Boolean {
    val prefs = getSharedPreferences(PrivacyPreferences.PREF_NAME, MODE_PRIVATE)
    // 初回起動時のみ表示
    return !prefs.getBoolean(PrivacyPreferences.KEY_CONSENT_DIALOG_SHOWN, false)
}

fun saveConsent(consent: Boolean) {
    val prefs = getSharedPreferences(PrivacyPreferences.PREF_NAME, MODE_PRIVATE)
    prefs.edit {
        putBoolean(PrivacyPreferences.KEY_LEARNING_CONSENT, consent)
        putLong(PrivacyPreferences.KEY_CONSENT_DATE, System.currentTimeMillis())
        putString(PrivacyPreferences.KEY_CONSENT_VERSION, "v1.0")
        putBoolean(PrivacyPreferences.KEY_CONSENT_DIALOG_SHOWN, true)
    }
}
```

### 5.2 Material Design 3 コンポーネント

#### ダイアログ

```kotlin
MaterialAlertDialogBuilder(context, R.style.ThemeOverlay_App_MaterialAlertDialog)
    .setTitle("🔒 学習機能について")
    .setMessage(...)
    .setPositiveButton("同意する") { _, _ -> ... }
    .setNegativeButton("同意しない") { _, _ -> ... }
    .setNeutralButton("ポリシーを見る") { _, _ -> ... }
    .setCancelable(false)  // 戻るボタンで閉じない
    .show()
```

#### Switch（設定画面）

```xml
<androidx.preference.SwitchPreferenceCompat
    android:key="learning_enabled"
    android:title="学習機能を使用する"
    android:summary="OCR認識精度を向上させます"
    android:defaultValue="false"
    app:iconSpaceReserved="false" />
```

### 5.3 アニメーション

#### ダイアログ表示アニメーション

```kotlin
// Fade in animation
dialog.window?.attributes?.windowAnimations = R.style.DialogAnimation

// styles.xml
<style name="DialogAnimation">
    <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
    <item name="android:windowExitAnimation">@android:anim/fade_out</item>
</style>
```

### 5.4 多言語対応

#### 文字列リソース構成

```
res/
  values/
    strings_privacy.xml        # 英語（デフォルト）
  values-ja/
    strings_privacy.xml        # 日本語
```

#### 例

```xml
<!-- values/strings_privacy.xml -->
<string name="privacy_consent_title">About Learning Feature</string>
<string name="privacy_consent_message">This app stores your correction...</string>

<!-- values-ja/strings_privacy.xml -->
<string name="privacy_consent_title">学習機能について</string>
<string name="privacy_consent_message">このアプリは、OCR認識の精度向上のため...</string>
```

---

## 6. ユーザーフロー詳細

### 6.1 初回起動（学習機能同意）

```
ステップ1: アプリ起動
  MainActivity.onCreate()
    ↓
ステップ2: 権限チェック
  if (!hasAllPermissions()) {
    requestPermissions()  // 既存処理
  }
    ↓
ステップ3: 同意ダイアログ表示チェック
  if (shouldShowConsentDialog()) {
    showPrivacyConsentDialog()
  }
    ↓
ステップ4: ユーザー選択
  [同意する]
    → saveConsent(true)
    → LocalCorrectionManager.setEnabled(true)
    → ダイアログ閉じる

  [同意しない]
    → saveConsent(false)
    → LocalCorrectionManager.setEnabled(false)
    → ダイアログ閉じる

  [ポリシーを見る]
    → startActivity(PrivacyPolicyActivity)
    → ダイアログは背景に残る
```

### 6.2 設定画面からデータ削除

```
ステップ1: 設定画面を開く
  SettingsActivity
    ↓
ステップ2: "学習データを削除"をタップ
  Preference.setOnPreferenceClickListener
    ↓
ステップ3: 削除確認ダイアログ表示
  showDeleteConfirmationDialog()
    ↓
ステップ4: ユーザー選択
  [キャンセル]
    → ダイアログ閉じる

  [削除]
    → LocalCorrectionManager.clearAllData()
    → Toast("削除しました")
    → パターン数表示を更新（0件）
```

---

## 7. アクセシビリティ対応

### 7.1 スクリーンリーダー対応

```kotlin
// contentDescription の追加
button.contentDescription = "同意するボタン。学習機能を有効にします。"

// Talkback用のヒント
ViewCompat.setAccessibilityPaneTitle(view, "プライバシーポリシー")
```

### 7.2 高コントラスト対応

```xml
<!-- テキストは最低コントラスト比 4.5:1 -->
<color name="on_surface">#1C1B1F</color>
<color name="surface">#FFFBFE</color>

<!-- Material Design 3 が自動対応 -->
```

### 7.3 大きなフォント対応

```xml
<!-- sp 単位を使用（ユーザーのフォント設定に追従） -->
<TextView
    android:textSize="16sp"
    android:lineSpacingExtra="4dp" />
```

---

## 8. テスト項目

### 8.1 機能テスト

| テストケース | 期待結果 |
|------------|---------|
| 初回起動時、同意ダイアログが表示される | ✅ 表示される |
| 「同意する」をタップ、学習機能がONになる | ✅ 有効化される |
| 「同意しない」をタップ、学習機能がOFFになる | ✅ 無効化される |
| 「ポリシーを見る」でプライバシーポリシー画面が開く | ✅ 画面遷移 |
| 設定画面で学習機能ON/OFF切り替えができる | ✅ 切り替わる |
| 設定画面で学習パターン数が表示される | ✅ 正しい件数 |
| "学習データを削除"で削除確認が出る | ✅ ダイアログ表示 |
| 削除確認で「削除」をタップ、データが削除される | ✅ すべて削除 |
| 削除後、パターン数が0件になる | ✅ 0件表示 |

### 8.2 UI/UXテスト

| テストケース | 期待結果 |
|------------|---------|
| ダイアログのテキストが読みやすい | ✅ 16sp以上 |
| ボタンのタップ領域が十分（48dp以上） | ✅ Material基準 |
| スクリーンリーダーで操作可能 | ✅ TalkBack対応 |
| 横画面でもレイアウトが崩れない | ✅ 正常表示 |
| 大きなフォント設定でも読める | ✅ 正常表示 |

### 8.3 セキュリティテスト

| テストケース | 期待結果 |
|------------|---------|
| 同意状態が正しく保存される | ✅ SharedPreferences |
| アプリ再起動後も同意状態が保持される | ✅ 保持される |
| ダイアログを2回目以降は表示しない | ✅ 表示されない |

---

## 9. 実装スケジュール

### Week 1: UI実装

| 日 | タスク | 担当 |
|----|--------|------|
| Day 1 | strings_privacy.xml 作成 | - |
| Day 1-2 | dialog_privacy_consent.xml 作成 | - |
| Day 2-3 | PrivacyConsentDialog.kt 実装 | - |
| Day 3-4 | PrivacyPolicyActivity.kt 実装 | - |
| Day 4-5 | MainActivity.kt 修正（ダイアログ表示） | - |

### Week 2: 設定画面・テスト

| 日 | タスク | 担当 |
|----|--------|------|
| Day 1 | preferences_learning.xml 作成 | - |
| Day 1-2 | SettingsActivity.kt 修正 | - |
| Day 2-3 | データ削除機能実装 | - |
| Day 3-4 | UI/UXテスト | - |
| Day 4-5 | バグ修正・調整 | - |

---

## 10. リファレンス

### Material Design 3
- [Dialogs](https://m3.material.io/components/dialogs/overview)
- [Buttons](https://m3.material.io/components/buttons/overview)
- [Text fields](https://m3.material.io/components/text-fields/overview)

### Android Official
- [Request runtime permissions](https://developer.android.com/training/permissions/requesting)
- [Shared Preferences](https://developer.android.com/training/data-storage/shared-preferences)
- [Material Components for Android](https://github.com/material-components/material-components-android)

### Markwon Library
- [GitHub Repository](https://github.com/noties/Markwon)
- [Usage Guide](https://noties.io/Markwon/)

---

**作成**: Claude Sonnet 4.5 (2025-12-18)
**ライセンス**: MIT License
